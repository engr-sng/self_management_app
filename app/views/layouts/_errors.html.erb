<% if obj.errors.any? %>
  <div class="box-100 errors-text">
    <h3><%= obj.errors.count %> 件のエラーが発生しました</h3>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
